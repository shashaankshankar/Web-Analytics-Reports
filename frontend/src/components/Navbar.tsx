import { BarChart3, LogOut, ShieldCheck, UserRound } from 'lucide-react'; import type { Identity, PortalResource } from '../types';
export function Navbar({ identity, resource, onSwitch }: { identity: Identity; resource?: PortalResource; onSwitch?: () => void }) { const agency = identity.identity.role.startsWith('agency'); return <header className="topbar"><a className="brand" href="/portal"><span className="brand-mark"><BarChart3 size={19}/></span>Measurement Portal</a><div className="topbar-meta"><span className="pill"><ShieldCheck size={13}/> {agency ? 'Agency workspace' : 'Client workspace'}</span>{resource && <small>{resource.companyName} · {resource.canonicalDomain}</small>}<span className="pill"><UserRound size={13}/> {identity.identity.email}</span><span className="pill">{identity.identity.role.replaceAll('_',' ')}</span>{onSwitch && <button className="btn secondary" onClick={onSwitch}>Switch</button>}<a className="pill" href="/logout"><LogOut size={13}/> Sign out</a></div></header>; }
