# Requirement audit

Updated August 14, 2026. This is the evidence ledger for `Measurement and Reporting Platform.md`. A later stage is not treated as authorization to bypass an earlier hard gate.

## Current release boundary

| Scope | Status | Evidence or blocker |
| --- | --- | --- |
| Steps 1-6 | Implemented and approved | Shashaank Shankar approved the healthcare/privacy policy, consent, eleven explicit public routes, contract, mappings, and request semantics on August 12, 2026. Unknown routes and unimplemented route patterns remain prohibited. |
| Steps 7-9 | Implemented and live | The live site has one collection owner, advanced Consent Mode v2, approved semantic events, and a validated Resend request handoff. |
| Steps 10-13 | Implemented and observed live for all applicable events | The 35-check site suite passes, including a runtime integration test proving a consented successful response emits exactly one each of `form_submit`, `generate_lead`, and `appointment_request` with only allowlisted parameters. Live unsafe-URL and unsafe-fragment checks initialized no analytics. The GA4 Realtime API observed exactly one each of `form_start`, `cta_click`, `phone_click`, `form_submit`, `generate_lead`, and `appointment_request`. No mailto CTA exists, so `email_click` is not applicable. |
| Step 14 | Live | Measurement health exposes contract, stream, measurement ID, timezone, event, consent/governance, persistence, and collection states. |
| Phase 1 gate | **Passed** | Governance, routes, consent, semantics, privacy fixtures, all six applicable event receipts, and the production Resend handoff pass. `email_click` is documented as not applicable. |
| Steps 15-39 | Live | FastAPI, Cloud SQL schemas, versioned semantics, ADC credential abstraction, Admin/Data API adapters, fixed reports, exact provenance, daily/period facts, Scheduler, Tasks, workers, retries, replay/dead-letter state, idempotency, reconciliation, and data-quality states are deployed. |
| Phase 2 gate | **Passed** | A repeated production job returned `idempotentReplay: true`; all six report definitions pass GA4 compatibility; all facts link to executions; failed validation jobs replayed; quota, thresholding, restrictions, dimension loss, freshness, and errors are visible. A synthetic inaccessible second property dead-lettered without changing the healthy first assignment, then all fixtures were removed. |
| Steps 40-49 | Live | Stored product APIs and a private dashboard expose overview, fixed periods/comparisons, acquisition, conversion, landing pages, expected events, measurement health, and sync status. |
| Phase 3 gate | Passed for available approved metrics | Dashboard values resolve to stored snapshots, report executions, report version 1, and exact GA4 requests. Outcome metrics remain zero/unapproved rather than being inferred. |
| Step 50 | Complete | `ONBOARDING.md` documents the repeatable client onboarding path and hard gates. |
| First production milestone | **Passed** | One real client has approved privacy-safe measurement, complete applicable live event evidence, managed GA4 access, idempotent persisted synchronization, fixed-period reporting, a private dashboard, visible health, and documented onboarding. |
| Steps 51-57 | **Live and passed** | The agency portfolio, alerts, annotations, five-role authorization, tenant service context, initial 28 RLS policies, separate database roles, and adversarial API/service/database tests are live. A transaction-only second tenant proved cross-tenant reads and writes are denied. |
| Steps 58-60 | Live foundation | Google-verified request identities resolve to tenant memberships. The client view omits agency repair details, five roles are enforced, approved effective-dated goals are tenant-scoped and audited, and PDF reports come from the same stored snapshots as the dashboard. No client users or goals have been invented. |
| Step 61 | Live but inactive by configuration | Recurring-report schedules, idempotent delivery ledger, PDF attachment generation, provider error states, daily dispatcher, and secret-referenced recipients are deployed. Creation fails closed until an approved recipient, sender, and email credential exist. |
| Step 62 | Live in restricted Testing mode | Google Auth Platform is External/Testing with one approved operator test user and exactly `analytics.readonly`. The private agency console now exposes the connection control. A separate public callback-only Cloud Run service exposes only health and callback routes; the reporting service remains private. Signed expiring state, PKCE, offline access, KMS-encrypted verifiers/refresh tokens, re-consent errors, provider revocation, token-deleting offboarding, and audit events are live. The approved test grant completed and property discovery returned one read-only property, `549268305`; because it does not match House of Dental property `549721844`, it was never assigned and was subsequently revoked with its encrypted token deleted. Public publishing remains disabled pending approved platform legal pages, an owned authorized domain, Google verification, and an explicit production decision. |
| Phase 5 retention/deletion | **Live and tested** | Configurable retention, a 30-day reversible offboarding grace period, immediate sync/report disablement, deletion preview, scheduled purge, exclusive credential deletion, and audit tombstones passed a temporary production-tenant deletion test. |
| Steps 63-68 | Runtime live; Search Console connected | Tenant-isolated Google Ads, Search Console, call-outcome, CRM/booking, and revenue schemas are deployed with version-pinned secret resolution, approval/audit workflow, an isolated daily source scheduler, idempotent sync provenance, privacy-validated first-party outcome ingestion, and paid/search/business reporting APIs. The House of Dental URL-prefix Search Console property is verified, the runtime service account and operator are owners, the pinned ADC source is approved, and the first production sync completed. Its new property has no approved historical rows and therefore reports `partial_data`/unavailable rather than zero. Ads, call tracking, and CRM remain `not_configured`. |
| Step 69 | Not justified | BigQuery raw-event analytics is optional and the plan says to add it only when aggregate reporting is insufficient. |

## Live production evidence

- Private Cloud Run revision `measurement-reporting-platform-00047-ftn` and callback-only revision `measurement-oauth-callback-00006-kz9` each serve 100% of their service traffic with OAuth client-secret version 2 and image `362d4d7`. The private service returns authenticated readiness 200 and rejects unauthenticated requests with HTTP 403. The public callback service returns health 200, missing-parameter callback 400, and 404 for root, readiness, docs, agency, and API routes. Its only invoker is `allUsers`; the private service's sole direct service-account invoker remains `measurement-scheduler`. On-Demand scan `72e10649-0ae6-4260-89c3-2b9f9c4d76ac` found zero critical/high and zero fixable critical/high vulnerabilities.
- `/ready` reports the `measurement` Cloud SQL database migrated and ready.
- GA4 Admin API verifies property `549721844`, web stream `15427015396`, measurement ID `G-TC66MQQ0T7`, and timezone `America/New_York`.
- Cloud Scheduler and Cloud Tasks completed all five fixed-period jobs on revision `00010-vd5` with HTTP 200; queue and failed-job counts returned to zero.
- Period boundaries use the GA4 property timezone. At the verification time, the latest complete date was `2026-08-12`; current-local-day activity is excluded.
- Re-dispatching the exact `28d` schedule key returned `idempotentReplay: true` for job `dbb74a68-014c-57d7-b30f-a0679d7bc5c5`.
- Source requests and metadata are stored on every new execution. Daily property, channel, page, and event facts are populated when rows exist. All seven mappings are approved; canonical facts populate only from observed source events.
- Cloud SQL backups, point-in-time recovery, 30 retained backups, and seven days of transaction logs are enabled. Automated backup `1786662000000` also passed a full isolated restore drill: the recovered database contained every migration, all 39 RLS policies, the three separated roles, 44 report executions, and 220 report snapshots; deterministic execution/snapshot hashes matched production, and the temporary instance was deleted after error-free creation, restore, and deletion operations.
- On August 13, 2026, an additional explicitly authorized consented production test made exactly one contact POST and returned the visible success state `Your message was sent. We'll get back to you soon.` after the Worker received an accepted Resend response. The message stated that it was a production test to disregard and did not create or confirm an appointment. Analytics consent was stored as granted; inbox placement remains independently unverified.
- On August 13, 2026, an explicitly authorized consented production test returned the same success state. The GA4 Realtime Data API then returned exactly one each of `form_submit`, `generate_lead`, and `appointment_request` at `minutesAgo=00`.
- Cloudflare Worker version `aae99152-f821-4aec-bf3c-0db2ebb533ef` explicitly allows approved static fragments such as `/contact#book` but continues to fail closed for unknown fragments such as `#patient-12345`.
- Production PostgreSQL now has 39 tenant RLS policies and three separated roles. A temporary second organization passed cross-tenant API, service, control-plane, external-source, read, and write denial tests, then rolled back without persisted fixture data.
- The scheduler now enumerates approved assignments and produced five fixed-period tasks for the live assignment. All five completed successfully on the per-assignment worker path. A synthetic inaccessible second property dead-lettered independently while the first property's latest success and zero-failure state remained unchanged.
- External-source sync provenance stores deterministic request/response hashes, row counts, reconciliation totals, idempotent replay state, and failures. A synthetic Ads fixture persisted normalized facts, replayed idempotently, and was fully removed through offboarding.
- Authenticated production requests return 200 for `/agency`, `/dashboard`, tenant-scoped goals, annotations, portfolio, and the PDF report. The live PDF is one page, parses successfully, and contains the stored reporting content; no goal exists until an authorized user supplies a real target.
- Cloud KMS key `oauth-refresh-tokens` is enabled, grants only the runtime identity encrypt/decrypt access, and rotates every 90 days. The external Google Auth Platform app is in Testing mode with one operator test user, exactly `analytics.readonly`, and a callback-only web client whose three inputs are version-pinned from Secret Manager. The client secret was rotated without downtime: version 2 is active on both services and version 1 is disabled in Google Auth Platform and Secret Manager. Live status reports `configured: true`, `enabled: true`, `productionApproved: false`, and zero stored OAuth connections. The test grant discovered `properties/549268305`, was not assigned because the approved production property is `properties/549721844`, and was then revoked with its token deleted; the existing ADC assignment remains active.
- `measurement-daily-sync`, `measurement-retention`, and `measurement-external-source-sync` are enabled. `measurement-report-dispatch` is deliberately paused per operator instruction. After a configuration inspection exposed the prior trigger value, it was rotated twice: newline-bearing version 7 was rejected and disabled, newline-free version 8 is pinned to production and all four scheduler headers, and versions 1 through 7 are disabled. A signed version-8 schedule created five tasks, all five workers returned HTTP 200, and the queue drained.
- Scheduler and Cloud Tasks use the dedicated `measurement-scheduler` invoker, while `analytics-reporting-reader` remains the runtime/data identity and can enqueue work without directly invoking the service. Cloud Monitoring has active operator notification plus ten policies for private-service and public-callback errors, multi-region callback availability, latency, task/scheduler failures, and database availability/capacity. A three-region HTTPS/JSON uptime check probes the public callback every minute, and its status is included in the five-panel production dashboard.
- The runtime identity has only `roles/telemetry.tracesWriter` and `roles/serviceusage.serviceUsageConsumer` for tracing. Both current revisions export synchronous, privacy-safe OpenTelemetry spans through the Google Telemetry API. Cloud Trace proves route-template spans for readiness, scheduling, all reporting workers, health, and the OAuth callback; their strict label set contains no URL, query string, request/response body, header, email, OAuth code/state, or exception message.
- The `_Default` Logging sink exclusion `oauth_callback_query_privacy` matches callback requests containing query strings. After propagation, a fresh OAuth-shaped probe produced zero retained request-log entries while the sanitized callback trace path remained available.
- Cloud SQL is regional HA, deletion-protected, encrypted-only, connector-only, retains 30 automated backups with seven-day PITR, and has query diagnostics enabled without client-address capture. Maintenance uses the stable production channel on Sunday at 06:00 UTC. The default log bucket retains 180 days.
- Project-level Data Access audit logging covers Admin Read, Data Read, and Data Write for all Google services with no exempt principals. All project service accounts have zero user-managed keys, and organization policies prevent service-account key creation and upload.
- `measurement-external-source-sync` is enabled for 5:00 AM Eastern. The original forced signed run returned 200 with zero approved connections, proving the fail-closed path; after Search Console approval, a second signed run queued and completed its source worker with HTTP 200.
- Production external-source status returns `partial_data` for the newly verified Search Console property and `not_configured` for Google Ads, call tracking, and CRM/booking. Search, paid, and business APIs return null/unavailable with caveats rather than fabricated zero outcomes when no approved rows exist. Search Console executions are always labeled partial top-row coverage because its API does not guarantee complete dimension rows.
- The plain local test command passes 58 tests, including the trace privacy allowlist, guardrailed tracking/lead-drop decisions, access-revocation classification, stale-data timing, contract-version evaluation, the enabled-Testing/false-public-approval OAuth boundary, encrypted authorization-state persistence, and public callback-only route isolation. The authenticated production dashboard renders the external-source, confirmed-outcome, paid, search, OAuth, and operator-alert controls.
- After connector enforcement and invocation-IAM tightening, authenticated readiness returned 200, unauthenticated readiness returned 403, and forced non-report daily sync, retention, and Search Console schedule/worker requests all returned HTTP 200. Report dispatch was not invoked and remains paused.
- After the final private/callback revisions reached 100% traffic, fresh forced runs of daily sync, retention, and external-source sync again returned HTTP 200; the task queue drained to zero. Report dispatch remained paused and was not invoked.
- Callback uptime samples from Iowa, Oregon, and Virginia all returned `true` after provisioning. Policy `7680340487027202507` independently alerts the operator when at least two regions fail for two minutes; the callback 5xx policy remains active for request failures.
- Step 52 has an explicit live path for all seven required conditions: `tracking_stopped`, `lead_count_unexpected_drop`, `contract_outdated`, `sync_failed`, `ga4_access_revoked`, `data_stale`, and `measurement_incompatibility`. The post-deploy five-period production run evaluated the period-specific paths without opening false alerts; live portfolio compatibility is `compatible`, failed jobs are zero, and action-required is false.

## Additional operational evidence

1. The ignored local House of Dental Resend key returns HTTP 401. The deployed website Worker uses a different valid secret, proven by provider acceptance, but Cloudflare correctly does not permit that secret to be read back or copied into the reporting service.
2. Inbox placement remains a separate office-inbox check; Resend acceptance is already proven.
3. DebugView/Tag Assistant parameter inspection is optional additional UI evidence because the live Data API receipt plus code and fixture proof cover the Phase 1 gate.

These items do not reopen the passed Phase 1 gate.
