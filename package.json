{
  "name": "measurement-reporting-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "validate:privacy": "node measurement/privacy/validate.mjs",
    "validate:platform": "node --test tests/**/*.test.mjs",
    "check": "npm run validate:privacy && npm run validate:platform",
    "dev": "node apps/web/server.mjs",
    "db:migrate": "node scripts/apply-migration.mjs"
  },
  "dependencies": {
    "google-auth-library": "^11.0.0",
    "pg": "^8.16.3"
  }
}
